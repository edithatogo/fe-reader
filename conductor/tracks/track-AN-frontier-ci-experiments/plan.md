# Frontier CI Experiments Plan

1. Materialise files for this track.
2. Run `python3 scripts/strict_contract_check.py`.
3. Run `python3 scripts/ci_policy_check.py`.
4. Update `docs/v9-coding-agent-start-here.md` with any missing first PRs.
5. Mark advisory checks as hard only after baselines exist and an ADR approves the promotion.
