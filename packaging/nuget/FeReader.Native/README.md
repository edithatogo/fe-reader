# FeReader.Native

Preview .NET wrapper for Fe Reader's native C ABI.

This Wave 0 package is intentionally plan-only. It exposes C ABI version, contract metadata and the no-write plan probe. It does not expose document mutation, apply, file IO, rendering, platform automation, plugins, ML or PDF parsing.

The native library must be supplied by the host application or future platform packages.
