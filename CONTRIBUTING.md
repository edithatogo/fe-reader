# Contributing to Fe Reader

Start with `docs/v6-coding-agent-start-here.md`, `AGENTS.md`, `conductor/waves.yaml` and `contracts/README.md`.

Contributions must preserve core boundaries, include tests or evidence, and update traceability/ADR docs when they change public contracts or architecture.
