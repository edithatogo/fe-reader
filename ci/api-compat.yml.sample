name: api-compat
on: [push, pull_request]
jobs:
  api-compat:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo install cargo-semver-checks || true
      - run: scripts/api_compat_check.sh
